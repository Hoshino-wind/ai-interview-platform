# 项目概述

> 这个文件用于记录项目的概述信息，帮助 Claude 快速了解项目。

## 项目名称

AI 开放式面试评估平台

## 项目描述

一个基于 AI 的技术面试评估系统，用于评估候选人的真实问题解决能力。

**核心理念**：评估工具使用能力，而非限制工具使用
- 不做反作弊：允许候选人使用 AI、搜索引擎、文档等任何工具
- 评估过程而非仅结果：记录候选人如何使用工具解决问题的完整过程
- 真实工作模拟：现代开发工作本身就是"人+工具"协作

**评分机制**：
- 自动客观评测（40%）：正确性、完整度、边界、性能
- AI 结构化评估（20%）：方案质量、推理链、工具协作
- 面试官评估（40%）：追问表现、可解释性、岗位契合度

**适用岗位**：前端、后端、测试、DevOps（MVP 阶段）

## 技术栈

### 前端
- Next.js + TypeScript
- SSR/CSR 混合渲染

### 后端
- NestJS + TypeScript
- 模块化单体架构

### 数据存储
- PostgreSQL：业务主库（事务、审计、配置、评分结果）
- Redis：队列、限流、缓存、幂等键
- MinIO/S3：提交快照、运行日志、导出报告

### 异步任务
- BullMQ：评测任务队列
- evaluation-worker：客观评测任务消费
- ai-worker：AI 质评与报告生成

### 代码执行
- Docker：容器沙箱执行候选人代码
- cgroup + seccomp：资源限制与安全隔离

### 可观测性
- OpenTelemetry：链路追踪
- Prometheus：指标采集
- Grafana：可视化监控

## 目录结构

```
ai-interview-platform/
├── apps/
│   ├── web-app/              # 前端应用（候选人工作台、面试官复核台、管理后台）
│   ├── api-server/           # 后端 API 服务
│   ├── evaluation-worker/    # 评测任务 Worker
│   ├── ai-worker/            # AI 评估 Worker
│   └── sandbox-runner/       # 沙箱执行器
├── packages/
│   ├── shared-types/         # 共享类型定义
│   ├── scoring-engine/       # 评分引擎
│   └── audit-sdk/            # 审计 SDK
├── infra/
│   ├── docker/               # Docker 配置
│   ├── k8s/                  # Kubernetes 配置
│   └── observability/        # 可观测性配置
├── docs/
│   ├── PRD.md                # 产品需求文档
│   ├── TECHNICAL_DESIGN.md   # 技术设计文档
│   └── DESIGN_PHILOSOPHY_UPDATE.md  # 设计理念更新说明
├── .tasks/
│   └── current.md            # 当前任务状态
└── .claude-summary.md        # 项目概述（本文件）
```

## 开发说明

### 架构原则
- 模块化单体：核心能力集中在一个后端服务，降低早期复杂度
- 异步评测：评测与 AI 质评通过队列异步执行，不阻塞在线交互
- 沙箱隔离：候选人代码在独立沙箱节点执行，与核心服务网络隔离

### 核心模块
1. `auth`：登录、SSO、RBAC、会话管理
2. `position-modeling`：岗位模板、能力项、评分权重
3. `question-bank`：锚点题、个性化模板、验收标准
4. `question-package`：按 70/30 规则生成题包
5. `interview-session`：候选人面试会话、状态机
6. `submission`：提交记录与版本快照（含工具使用轨迹）
7. `evaluation`：客观评测任务、结果聚合
8. `ai-assessment`：五段式输出、工具使用能力评估
9. `review`：面试官追问、调分、最终裁决
10. `reporting`：候选人报告、岗位对比
11. `compliance-audit`：授权、访问日志、调分审计

### 数据设计要点
- `submissions` 表记录：思考过程、迭代理由、AI prompt 使用
- `ai_assessments` 表包含：五段式评估 + 过程质量评估（4个维度）
- 所有评分必须有可定位的证据支撑（提交版本、行号、用例 ID）

### 安全与合规
- 候选人代码执行必须与业务系统隔离
- 授权、访问、调分和导出行为全量留痕
- 审计日志不可篡改，支持按候选人与操作人双维度追溯
- 默认保留期：评估主数据 180 天，审计日志 365 天

### 交付计划（8 周 MVP）
- 周 1-2：基础能力（权限、岗位模型、题库、会话、提交）
- 周 3-4：评测闭环（队列、Worker、沙箱、Gate 判定）
- 周 5-6：AI 质评与复核（五段式、调分、报告）
- 周 7-8：合规与试跑（授权、审计、清理、压测）

## 工作日志

### 2026-02-16
- ✅ 完成设计理念更新：从"反作弊"转向"工具使用能力评估"
- ✅ 更新 PRD 和技术设计文档
- ✅ 在 AI 评估协议中增加工具使用能力评估维度（4个维度）
- ✅ 创建设计理念更新说明文档

### 2026-02-20
- ✅ 完善项目概述文档
