# Sandbox Runner

Standalone stub sandbox service for local development.

Endpoints:
- `GET /health`
- `POST /execute`

Run locally:
- `pnpm --filter @ai-interview/sandbox-runner dev`
- `pnpm --filter @ai-interview/sandbox-runner build && pnpm --filter @ai-interview/sandbox-runner start`
